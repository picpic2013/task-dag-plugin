# 03 文档技能与测试矩阵收口

## 任务目标

让 README、SKILL、迁移文档和测试矩阵全部与“新协议唯一主路径”一致。

## 开发任务

1. README 删除旧接口引用
2. SKILL 删除旧接口引用和旧流程示例
3. MIGRATION 文档改成“已移除接口”而不是“仍保留兼容”
4. 测试矩阵只保留新协议

## 执行细则

### 文档要求

- 不再出现“spawn 后 wait”
- 不再出现 `task_dag_update` 主路径
- 不再出现 `TASK_COMPLETE/...` 文本协议示例

### 测试要求

- 工具注册列表测试
  - 确认旧工具不存在
- 主路径测试
  - 父直接执行
  - 单子 agent 单任务
  - 单 session 多任务
  - hook 收尾
  - continuation
  - reconcile

## 测试方案

1. 全量 `node test_runner.mjs`
2. `npm run build`
3. 工具注册断言：
   - 不存在 `task_dag_wait`
   - 不存在 `task_dag_notify`
   - 不存在 `task_dag_update`

## 预期结果

- 文档与实现一致
- 模型可见入口只剩新协议
- 测试矩阵完全覆盖新架构
